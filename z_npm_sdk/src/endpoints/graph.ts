import { RequestBuilder } from '../http-builder.js';

/**
 * Graph API (stub)
 * - 서버 라우트가 미정(TODO)이므로, 향후 구현 시 이 파일을 확장한다.
 */
export class GraphApi {
  constructor(private rb: RequestBuilder) {}

  // TODO: add vector upsert/search/delete when server routes are finalized
}
