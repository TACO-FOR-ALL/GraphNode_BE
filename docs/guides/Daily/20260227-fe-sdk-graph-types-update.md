# 작업 상세 문서 — FE SDK Graph 생성 옵션 및 타입 변경

## 📌 메타 (Meta)
- **작성일**: 2026-02-27 KST
- **작성자**: 백엔드 팀 (AI Agent)
- **스코프 태그**: [FE] [SDK]

---

## 📝 TL;DR (핵심 요약)
- **목표:** Multi-source Graph 생성을 위한 프론트엔드 SDK(`z_npm_sdk`) 파라미터 간소화 및 `numMessages` 타입 롤백
- **결과:** `GraphGenRequestOptions` 내에서 `summaryLanguage`, `inputType`, `extraS3Keys` 삭제. `numSections` 명칭을 기존 `numMessages`로 원복.
- **영향 범위:** 클라이언트 및 프론트엔드 어플리케이션에서 `graphAi.generateGraph` 호출 시 불필요한 설정 옵션 제거. 이전 `numMessages` 코드와 100% 호환.

---

## 📌 배경 / 컨텍스트

### 요구 사항
- AI 서버가 이제 여러 개 출처(채팅 + 노트)의 Graph 혼합 생성을 지원함. 
- 이 과정에서 AI가 파일 포맷을 스스로 판별(Auto-detect)하므로, `inputType`이나 다른 S3 키들을 FE가 조작할 필요가 없어짐. 언어 설정 역시 사용자 기본 정보를 읽어 BE에서 주입.
- 기존 DB와의 정합성 및 FE UI 코드의 파괴적 변경을 피하기 위해 `numSections`로 이름을 바꾸었던 것을 철회하고 이전 이름인 `numMessages`로 하위 호환성 유지 필요.

---

## 📦 산출물

### 📄 수정된 파일
- `z_npm_sdk/src/types/graphAi.ts` — `GraphGenRequestOptions` 인터페이스에서 불필요한 속성 제거
- `z_npm_sdk/src/types/graph.ts` — `GraphNodeDto`의 `numSections`를 `numMessages`로 롤백
- `z_npm_sdk/src/endpoints/graphAi.ts` 및 `graph.ts` — JSDoc에서 `numMessages`로 명칭 롤백

---

## 🔧 상세 변경 (Method/Component)

### ✏ 수정 (Modified)
- `z_npm_sdk/src/types/graphAi.ts` — `GraphGenRequestOptions`에서 `summaryLanguage` 매개변수가 삭제되었습니다. AI의 요약 언어는 백엔드에서 사용자 환경설정에 따라 자동으로 결정됩니다.
- `z_npm_sdk/src/types/graph.ts` — 반환받는 그래프 노드의 메시지 개수를 나타내는 필드가 다시 `numMessages`로 변경되었습니다. (Multi-source로 넘어와서도 노트의 문단 개수 등을 동일하게 `numMessages` 필드에 담습니다.)

---

## 🚀 재현/실행 절차 (Onboarding)
- 기존에 프론트엔드에서 `generateGraph` 함수를 호출할 때 `summaryLanguage` 같은 인자를 넘기던 부분을 제거하기만 하면 정상 동작합니다.

---

## ⚠ 리스크 / 이슈 / 트러블슈팅
- 없음 (하위 호환성 100% 보장. 백엔드의 Mapper 레이어(`.mapper.ts`)에서 AI 응답을 매핑하여 프론트엔드 단에 투명하게 전달함)
